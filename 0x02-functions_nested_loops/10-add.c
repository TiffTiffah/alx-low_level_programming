#include <stdio.h>
#include "main.h"

/**
 * add - adds two integer and returns the results
 * @a: first integer
 * @b: second integer
 *
 * Return: the result
 */
int add(int a, int b)
{
	return (a + b);
}
